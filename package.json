{
  "name": "moar-cars-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm install --prefix frontend && npm run build --prefix frontend && node copy-build.js",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "nodemailer": "^10.0.10",
    "sequelize": "^6.37.8",
    "mysql2": "^3.12.0",
    "sqlite3": "^6.0.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "overrides": {
    "qs": "^6.14.0",
    "tar": "^7.5.2",
    "uuid": "^11.1.1",
    "nodemailer": "^10.0.10"
  }
}
